const fs = require('fs'); const path = require('path'); require('dotenv').config(); const FigmaService = require('../designetica-services/figmaService.js'); (async () => { try { const figma = new FigmaService(); const result = await figma.generateAtlasHeroFromFigma('14647:163530'); console.log('Success! Got component of length:', result.length); if (result.includes('figma-alpha-api')) console.log('✅ Real Figma image URL found!'); else console.log('⚠️ Fallback component'); } catch(e) { console.error('Error:', e.message); } })();
