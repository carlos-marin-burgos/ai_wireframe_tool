# NPM Configuration for Backend
# This helps prevent dependency conflicts and ensures consistent installations

# Use exact versions to prevent unexpected updates
save-exact=true

# Include development dependencies in production
production=false

# Use package-lock.json for consistent installs
package-lock=true

# Prevent scripts from other packages from running during install
ignore-scripts=false

# Fund messages can be distracting during development
fund=false
