# Production environment variables (point to deployed Azure Functions backend)
# IMPORTANT: Do not set these to localhost in real production; that caused OAuth failures.
# Frontend (Static Web App) calls Functions backend at this base URL.
VITE_API_ENDPOINT=https://func-original-app-pgno4orkguix6.azurewebsites.net/api
VITE_BACKEND_BASE_URL=https://func-original-app-pgno4orkguix6.azurewebsites.net

# Optional hardening flags (can be used in code for conditional logic)
VITE_ENV=production
# NODE_ENV is managed by Vite build; no need to override.
